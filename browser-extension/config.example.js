// Copy this file to config.js and fill in local-only values.
// config.js is gitignored so the bridge token is not committed.
const BRIDGE_TOKEN = 'CHANGE_ME';
const GLM_WEB_ORIGIN = 'https://YOUR-INTERNAL-GLM-HOST';
